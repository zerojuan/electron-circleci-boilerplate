machine:
  node:
    version: 5.2.0
dependencies:
  override:
    - npm set progress=false
    - npm install
    - npm install rimraf -g
deployment:
  release:
    tag: /release-*/
    commands:
      - ./build/release-to-github.sh
