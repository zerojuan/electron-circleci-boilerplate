machine:
  node:
    version: 5.2.0
dependencies:
  override:
    - npm set progress=false
    - npm install
    - npm install rimraf -g
deployment:
  release:
    tag: /v[0-9]+(\.[0-9]+)*/
    commands:
      - ./build/release-to-github.sh
